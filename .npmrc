# 모노레포 도구(typescript, eslint, vitest 등)를 모든 워크스페이스에서 해석할 수 있도록 평탄화 설치
node-linker=hoisted
# 현재 환경 Node는 v22. 기술 스택 문서는 Node 24 LTS를 권장하나 22에서 정상 동작하므로 강제하지 않음
engine-strict=false
# Next.js / 빌드 도구가 자주 쓰는 패키지의 빌드 스크립트 허용
strict-peer-dependencies=false
