import type { FastifyInstance } from "fastify";

/**
 * Story 3.5 — GET /api/v1/qna/questions/:slug 상세 + 조회수(Redis 버퍼 + view-flush 큐).
 * 스텁: Story 3.5 담당 에이전트가 채운다.
 * 응답은 questionDetailResponseSchema(answers 포함, derivedStatus 도출).
 */
export async function registerQnaDetailRoutes(_app: FastifyInstance): Promise<void> {
  // TODO(Story 3.5): GET /qna/questions/:slug 구현
}
