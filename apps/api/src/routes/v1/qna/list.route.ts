import type { FastifyInstance } from "fastify";

/**
 * Story 3.2 — GET /api/v1/qna/questions 목록.
 * 스텁: 구현은 Story 3.2 담당 에이전트가 이 파일에서 채운다.
 * (questionListQuerySchema 검증 · status 필터 · 오프셋 페이지네이션 · derivedStatus 도출)
 */
export async function registerQnaListRoutes(_app: FastifyInstance): Promise<void> {
  // TODO(Story 3.2): GET /qna/questions 구현
}
